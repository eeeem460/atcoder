・int の範囲を超えていないか, 掛け算は慎重に LINF注意, (1LL << i)
  #difine int long long + signed main(void)
・変数の参照とコピーを間違えていないか(for auto)
・関数の型に注意（accumulateの返り値の型は初期値の型）
・コーナーケースに注意
・小数点誤差、cout << fixed << setprecision(10);
・< と <= の違い、変数の範囲
・配列の添え字 0スタートか1スタートか
・演算子の優先順位、否定の演算子！は！（）の形で使う, == より ビット演算の方が優先順位が低い
・mod M は M進数で考えてみる
・絶対値問題の中央値はa[(n-1)/n]でよい
・double は16桁まで表現できる、最大値は10^300ぐらいなので気にしない、初期値は0.0
・行列の添え字は（行、列）の順番に
・徐算の切上げ、(a + (b-1)) / b
・bool型のint型での値は, true は0以外, false は0 (true は必ずしも 1 ではない!!)
・配列をソートすると添え字との対応が崩れます
・(*itr).first と書く必要がある　演算子.txt を参照
・アルファベットは全部で２６文字.
・クエリ処理の場合は入力の途中で出力されるかも.
・mod は最後にまとめてとって出力
・return 0 を忘れない.
・場合の数DPの初期値で例えば dp[0 + 0] と dp[0 - 0] をしっかり区別する．